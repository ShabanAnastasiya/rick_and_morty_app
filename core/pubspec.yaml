name: core
version: 0.0.1
publish_to: none
description: Project core module

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  core_ui:
    path: ../core_ui
  data:
    path: ../data
  character_list:
    path: ../features/character_list

  get_it: ^8.0.3
  dio: ^5.4.0
  logger: ^2.0.2
  flutter_bloc: ^8.1.4
  observe_internet_connectivity: ^1.0.2
  easy_localization: ^3.0.7
  shared_preferences: ^2.5.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - resources/lang/